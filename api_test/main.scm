#lang scheme


(require http/request)
(require "../test/colorize.scm")

; pg_n
; pg_s
; py_n
; py_s



;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------


; in out -> req res

(define-values (in out) (connect-uri "http://localhost:3001"))


(pg_s "in ->")
(display in)
(display "\n")


(pg_s "out ->")
(display out)
(display "\n")


(display (start-request
    in
 	 	out
 	 	"1.1"
 	 	"GET"	 	 	 	 
 	 	"/koyn"
 	 	""))


(disconnect in out)




















;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------










;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------
