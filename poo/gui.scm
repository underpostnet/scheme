#lang scheme
(require ansi)
(require dyoo-while-loop)
(require "../test/colorize.scm")

;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------

; (clear-screen)
; (clear-line)
; (clear-field)
; (kill-line)

(define cont 0)

(display (clear-screen/home))

(while #t

  (pg_s (string-append "\n" (number->string cont) "\n test 1"))
  (display "\n test 2")
  (display "\n test 3 \n")
  (set! cont (+ cont 1))
  (sleep 1)
  (define input (read-line))
  (display (clear-screen/home))

  )

;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------





















;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------













;-------------------------------------------------------------------------------
;-------------------------------------------------------------------------------
